{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.cerberauth.com/identity-provider/draft/2024-09/category.schema.json",
  "title": "Category Definition",
  "description": "Schema for an individual category definition item in CerberAuth identity provider data.",
  "type": "object",
  "properties": {
    "identifier": {
      "type": "string",
      "description": "Unique machine-readable identifier of the category.",
      "pattern": "^[a-z0-9_]+$",
      "minLength": 1
    },
    "translationKey": {
      "type": "string",
      "description": "Translation key corresponding to internationalized name and description in i18n data.",
      "pattern": "^[a-z0-9_]+$",
      "minLength": 1
    }
  },
  "required": ["identifier", "translationKey"],
  "additionalProperties": false
}
